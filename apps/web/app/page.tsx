export default function HomePage() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui" }}>
      <h1>Monorepo listo</h1>
      <p>
        Next.js (web) + NestJS (API) + Prisma + PostgreSQL configurados en este
        monorepo.
      </p>
    </main>
  );
}

